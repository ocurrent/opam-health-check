(jbuild_version 1)

(executable
  ((name check)
   (modules (check))
   (libraries (lwt.unix))))

(executable
  ((name server)
   (modules (server diff))
   (libraries (tyxml cohttp-lwt-unix containers))))
