(jbuild_version 1)

(executable
 ((name server)
  (public_name opam-serve-all)
  (libraries
   (tyxml
    cohttp-lwt-unix
    containers
    opam-core
    nocrypto
    yaml
    cmdliner
    oca_lib))))

(rule
  (with-stdout-to config.ml
    (progn
      (echo "let name = {|opam-serve-all|}\n")
      (echo "let version = {|${version:opam-check-all}|}\n"))))
