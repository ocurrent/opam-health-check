(jbuild_version 1)

(library
 ((name backend)
  (wrapped false)
  (flags (:standard -safe-string -w A-44-33-4 -open Containers))
  (libraries
   (oca_lib
    cohttp-lwt-unix
    containers
    opam-core
    nocrypto.lwt))))
